package com.swipehire.app.ui.theme

import androidx.compose.ui.graphics.Color

// ---- SwipeHire "Aurora" palette ----
// A punchier, more saturated take on the original brand colors, built around
// an inky obsidian dark mode and a soft lavender-tinted light mode instead of
// flat black/white — the goal is a premium, editorial feel rather than a
// stock Material template.

// Violet — primary brand color
val Violet80 = Color(0xFFE3D9FF)
val Violet40 = Color(0xFF7C5CFC)
val Violet20 = Color(0xFF4B2FD1)
val VioletDeep = Color(0xFF2E1A8F)

// Mint — signal / success / "like"
val Mint80 = Color(0xFFB6FBE3)
val Mint40 = Color(0xFF17E4A8)
val Mint20 = Color(0xFF00A67D)

// Coral — "pass" / destructive
val Coral = Color(0xFFFF5470)
val CoralDeep = Color(0xFFD53A57)

// Amber — highlight / featured accent
val Amber = Color(0xFFFFB648)

// Sky — third aurora stop, used only in mesh gradients for depth
val Sky = Color(0xFF4FD3FF)

// Surfaces — dark mode is an inky obsidian, not pure black
val SurfaceDark = Color(0xFF0A0912)
val SurfaceDarkElevated = Color(0xFF14111F)
val CardDark = Color(0xFF1B1729)
val CardDarkHairline = Color(0xFF2E2842)

// Surfaces — light mode is a soft lavender-tinted white, not flat white
val SurfaceLight = Color(0xFFF6F4FF)
val CardLight = Color(0xFFFFFFFF)
val CardLightHairline = Color(0xFFE7E2F7)

// Text
val TextPrimaryDark = Color(0xFFF6F3FF)
val TextSecondaryDark = Color(0xFFAAA2C7)
val TextPrimaryLight = Color(0xFF171226)
val TextSecondaryLight = Color(0xFF6C6485)
