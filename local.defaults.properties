# Fallback so the project still builds if a teammate hasn't set up their own
# Maps API key yet. This file IS committed — put your real key in local.properties
# (already gitignored) as MAPS_API_KEY=your_real_key instead of editing this one.
MAPS_API_KEY=DEFAULT_API_KEY
